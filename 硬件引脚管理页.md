# STM32U575 引脚管理页

## 1. 文档用途

这份文档用于给当前工程做一页可长期维护的“引脚管理表”，方便后续继续接温湿度、舵机、按键、串口、SPI 设备时快速判断哪些脚已经占用、哪些脚还能扩展。

判定依据：

- 当前工程 CubeMX 配置：[01oled.ioc](/c:/Users/a7660/Desktop/428/01oled/01oled.ioc)
- 当前工程代码：
  - [Core/Src/main.c](/c:/Users/a7660/Desktop/428/01oled/Core/Src/main.c)
  - [App/Src/app.c](/c:/Users/a7660/Desktop/428/01oled/App/Src/app.c)
  - [Drivers/ILI9341/Src/ili9341.c](/c:/Users/a7660/Desktop/428/01oled/Drivers/ILI9341/Src/ili9341.c)
  - [Drivers/FT6336/Src/ft6336.c](/c:/Users/a7660/Desktop/428/01oled/Drivers/FT6336/Src/ft6336.c)
- 参考板级原理图：
  - `FS-STM32U5-v2.0核心板原理图(2023.07.29).pdf`
  - `FS-STM32db-v3.2底板原理图(2023.07.290).pdf`
- 参考旧 U575 工程板级配置：
  - `06-1_STM32U575_USART_Printf.ioc`
  - `硬件配置文档.md`

说明：

- 表中“当前状态”以“当前工程实际代码”为准，不只看 `01oled.ioc`。
- “可扩展方向”是合理建议，最终复用能力请以 CubeMX 当前 AF 映射为准。
- 部分“板载已连”来自原理图和旧工程验证，当前工程未必已经启用。

## 2. 状态说明

- `<span style="color:red">项目已用</span>`：当前工程已经实际使用
- `<span style="color:orange">板载已连/建议保留</span>`：原理图或旧工程表明该脚已有板载资源、调试用途或系统用途，当前工程未启用时也建议优先保留
- `未用`：当前工程未使用，且原理图中无必须保留用途，可优先作为扩展脚

## 3. 当前工程外设总览

| 外设/功能 | 引脚 | 当前用途 | 备注 |
| --- | --- | --- | --- |
| ADC1 | `PA3` | 光照传感器模拟输入 `ADC1_IN8` | 当前主数据输入 |
| SPI1 | `PA5/PA6/PA7` | ILI9341 LCD 通讯 | `SCK/MISO/MOSI` |
| GPIO | `PA4` | LCD `DC` | 手动 GPIO 控制 |
| GPIO | `PA8` | LCD `RST` | 手动 GPIO 控制 |
| GPIO | `PA11` | FT6336 `TP_RST` | 手动 GPIO 控制 |
| I2C1 | `PB6/PB7` | FT6336 触摸通讯 | 也适合后续扩展 I2C 器件 |
| USART1 | `PA9/PA10` | 调试串口 | `115200 8N1` |

## 4. 板级资源提示

结合核心板与底板原理图，以及旧 U575 工程，可额外确认这些板级资源长期值得关注：

| 资源 | 可能对应引脚 | 状态建议 | 说明 |
| --- | --- | --- | --- |
| 板载 LED | `PC13` | 建议保留 | 旧工程已验证为 LED 输出 |
| 舵机 PWM 口 | `PA1` | 建议优先 | 旧工程用作 `TIM2_CH2` 驱动 SG90 |
| 物理按键/输入 | `PC8` | 建议保留 | 旧工程作为普通输入使用 |
| 外部中断输入 | `PC9` | 建议保留 | 旧工程作为 EXTI 输入使用 |
| 单总线/DHT11 口 | `PA12` | 可优先考虑 | 旧工程文档曾规划为单总线数据口 |
| SWD 调试 | `PA13/PA14` | 不建议挪用 | 下载调试强相关 |
| HSE 晶振 | `PH0/PH1` | 不建议挪用 | 系统时钟相关 |
| LSE 晶振 | `PC14/PC15` | 不建议挪用 | 32.768k 晶振相关 |
| BOOT0 | `PH3` | 建议保留 | 启动模式相关 |
| 复位 | `NRST` | 必须保留 | 系统复位 |

## 5. 引脚管理表

### 5.1 Port A

| 引脚 | 封装脚号 | 当前状态 | 当前/已知用途 | 可扩展方向 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `PA0` | `14` | 未用 | 当前工程未使用 | `GPIO`、`EXTI`、按键输入、模拟量采集 | 适合做普通输入类功能 |
| `PA1` | `15` | `<span style="color:orange">板载已连/建议保留</span>` | 旧工程用作 `TIM2_CH2` 舵机 PWM | `PWM`、定时器输出、GPIO | 如果后续接真实舵机，优先回到这个脚 |
| `PA2` | `16` | 未用 | 当前工程未使用 | `GPIO`、串口、定时器、普通控制输出 | 可作为备用通讯脚 |
| `PA3` | `17` | `<span style="color:red">项目已用</span>` | 光照传感器 `ADC1_IN8` | 不建议再复用 | 当前界面实时显示光照值 |
| `PA4` | `20` | `<span style="color:red">项目已用</span>` | LCD `DC` 控制脚 | 若未来换屏，可改作 `CS/GPIO` | 当前由软件手动拉高拉低 |
| `PA5` | `21` | `<span style="color:red">项目已用</span>` | `SPI1_SCK`，LCD 时钟 | 若显示方案变化，可复用于 `SPI1` 设备 | 当前为屏幕主时钟 |
| `PA6` | `22` | `<span style="color:red">项目已用</span>` | `SPI1_MISO` | `SPI1` 从设备输入 | 当前 LCD 驱动未读回，但总线已占用 |
| `PA7` | `23` | `<span style="color:red">项目已用</span>` | `SPI1_MOSI`，LCD 数据 | 若切回 Flash/多 SPI 从设备可共线 | 当前屏幕主数据输出 |
| `PA8` | `41` | `<span style="color:red">项目已用</span>` | LCD `RST` 复位脚 | 若移除 LCD，可做 `GPIO/PWM/MCO` | 当前手动复位屏幕 |
| `PA9` | `42` | `<span style="color:red">项目已用</span>` | `USART1_TX` 调试串口 | 不建议改动 | 程序日志输出依赖 |
| `PA10` | `43` | `<span style="color:red">项目已用</span>` | `USART1_RX` 调试串口 | 不建议改动 | 串口交互依赖 |
| `PA11` | `44` | `<span style="color:red">项目已用</span>` | FT6336 `TP_RST` | 若触摸方案更换，可做 `GPIO` | 当前只作触摸复位 |
| `PA12` | `45` | `<span style="color:orange">板载已连/建议保留</span>` | 参考旧工程，适合单总线外设 | `DHT11`、单总线、GPIO、控制输出 | 可作为温湿度口候选 |
| `PA13` | `46` | `<span style="color:orange">板载已连/建议保留</span>` | `SWDIO` | 不建议普通业务占用 | 下载调试必留 |
| `PA14` | `49` | `<span style="color:orange">板载已连/建议保留</span>` | `SWCLK` | 不建议普通业务占用 | 下载调试必留 |
| `PA15` | `50` | 未用 | 当前工程未使用 | `GPIO`、`SPI/NSS`、定时器、EXTI | 适合做片选或按键中断 |

### 5.2 Port B

| 引脚 | 封装脚号 | 当前状态 | 当前/已知用途 | 可扩展方向 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `PB0` | `26` | 未用 | 当前工程未使用 | `GPIO`、`ADC`、`PWM` | 适合模拟/脉宽输出类功能 |
| `PB1` | `27` | 未用 | 当前工程未使用 | `GPIO`、`ADC`、`PWM` | 可做第二路模拟采样或蜂鸣器 |
| `PB2` | `28` | 未用 | 当前工程未使用 | `GPIO`、`EXTI`、普通控制 | 建议先做通用数字 IO |
| `PB3` | `55` | 未用 | 当前工程未使用 | `GPIO`、`SPI`、定时器 | 可作为备用总线脚 |
| `PB4` | `56` | 未用 | 当前工程未使用 | `GPIO`、`SPI`、定时器 | 适合片选/中断输入 |
| `PB5` | `57` | 未用 | 当前工程未使用 | `GPIO`、`I2C/SMBus`、定时器 | 可做控制脚或第二总线尝试 |
| `PB6` | `58` | `<span style="color:red">项目已用</span>` | `I2C1_SCL` | 总线已占用，不建议改动 | 当前触摸控制器挂载其上 |
| `PB7` | `59` | `<span style="color:red">项目已用</span>` | `I2C1_SDA` | 总线已占用，不建议改动 | 后续 I2C 传感器也可继续共线 |
| `PB8` | `61` | 未用 | 当前工程未使用 | `GPIO`、`I2C`、串口、CAN 候选 | 适合扩展低速外设 |
| `PB9` | `62` | 未用 | 当前工程未使用 | `GPIO`、`I2C`、串口、CAN 候选 | 与 `PB8` 可组成一组外设口 |
| `PB10` | `29` | 未用 | 当前工程未使用 | `GPIO`、`UART`、`SPI2_SCK/I2C` 候选 | 适合备用通讯 |
| `PB12` | `33` | 未用 | 当前工程未使用 | `GPIO`、`SPI2_NSS`、EXTI | 适合片选脚 |
| `PB13` | `34` | 未用 | 当前工程未使用 | `GPIO`、`SPI2_SCK`、定时器 | 适合扩展第二 SPI |
| `PB14` | `35` | 未用 | 当前工程未使用 | `GPIO`、`SPI2_MISO`、PWM | 可作为输入采样或从机输入 |
| `PB15` | `36` | 未用 | 当前工程未使用 | `GPIO`、`SPI2_MOSI`、PWM | 适合扩展第二 SPI 数据输出 |

### 5.3 Port C

| 引脚 | 封装脚号 | 当前状态 | 当前/已知用途 | 可扩展方向 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `PC0` | `8` | 未用 | 当前工程未使用 | `GPIO`、`ADC`、模拟传感器 | 适合温湿度模拟/电压采样 |
| `PC1` | `9` | 未用 | 当前工程未使用 | `GPIO`、`ADC`、模拟传感器 | 可作为第二路模拟输入 |
| `PC2` | `10` | 未用 | 当前工程未使用 | `GPIO`、`ADC`、模拟传感器 | 适合连续采样类功能 |
| `PC3` | `11` | 未用 | 当前工程未使用 | `GPIO`、`ADC`、模拟传感器 | 可预留给电位器/电压检测 |
| `PC4` | `24` | 未用 | 当前工程未使用 | `GPIO`、`ADC`、低速控制 | 可做扩展输入输出 |
| `PC5` | `25` | 未用 | 当前工程未使用 | `GPIO`、`ADC`、低速控制 | 可做扩展输入输出 |
| `PC6` | `37` | 未用 | 当前工程未使用 | `GPIO`、`UART`、`PWM` | 适合输出控制或第二串口 |
| `PC7` | `38` | 未用 | 当前工程未使用 | `GPIO`、`UART`、`PWM` | 可与 `PC6` 成对使用 |
| `PC8` | `39` | `<span style="color:orange">板载已连/建议保留</span>` | 旧工程作为输入脚使用 | `GPIO`、按键输入、状态检测 | 如果接实体按键，这个脚优先考虑 |
| `PC9` | `40` | `<span style="color:orange">板载已连/建议保留</span>` | 旧工程作为 `EXTI` 输入使用 | `EXTI`、按键中断、触发输入 | 很适合事件类中断 |
| `PC10` | `51` | 未用 | 当前工程未使用 | `GPIO`、`UART`、`SPI3/SD` 候选 | 适合高速数字外设 |
| `PC11` | `52` | 未用 | 当前工程未使用 | `GPIO`、`UART`、`SPI3/SD` 候选 | 适合高速数字外设 |
| `PC12` | `53` | 未用 | 当前工程未使用 | `GPIO`、`UART`、`SPI3/SD` 候选 | 可与 `PC10/PC11` 组合使用 |
| `PC13` | `2` | `<span style="color:orange">板载已连/建议保留</span>` | 核心板板载 LED | `GPIO` 输出 | 推荐优先恢复为运行指示灯 |
| `PC14` | `3` | `<span style="color:orange">板载已连/建议保留</span>` | `OSC32_IN` | 不建议挪作普通 IO | LSE 晶振输入 |
| `PC15` | `4` | `<span style="color:orange">板载已连/建议保留</span>` | `OSC32_OUT` | 不建议挪作普通 IO | LSE 晶振输出 |

### 5.4 Port D / Port H / System

| 引脚 | 封装脚号 | 当前状态 | 当前/已知用途 | 可扩展方向 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `PD2` | `54` | 未用 | 当前工程未使用 | `GPIO`、串口、控制输出 | 可作为普通扩展脚 |
| `PH0` | `5` | `<span style="color:orange">板载已连/建议保留</span>` | `OSC_IN` | 不建议挪作普通 IO | HSE 外部晶振输入 |
| `PH1` | `6` | `<span style="color:orange">板载已连/建议保留</span>` | `OSC_OUT` | 不建议挪作普通 IO | HSE 外部晶振输出 |
| `PH3` | `60` | `<span style="color:orange">板载已连/建议保留</span>` | `BOOT0` | 不建议业务长期占用 | 启动模式配置脚 |
| `NRST` | `7` | `<span style="color:orange">板载已连/建议保留</span>` | 复位脚 | 不可作为普通 IO | 调试和上电可靠性关键 |

## 6. 推荐扩展优先级

如果后面继续加模块，推荐优先按下面思路选脚：

| 优先级 | 推荐引脚 | 适合方向 |
| --- | --- | --- |
| 高 | `PA1` | 舵机/电机 PWM |
| 高 | `PC8/PC9` | 按键、外部中断、事件输入 |
| 高 | `PA12` | DHT11/单总线/普通 GPIO |
| 高 | `PC0~PC5` | 新增模拟量、环境传感器、电压采样 |
| 中 | `PB8/PB9/PB10` | 第二组低速通信、控制脚 |
| 中 | `PB12~PB15` | 第二 SPI、片选、GPIO 扩展 |
| 中 | `PC10~PC12` | 高速数字总线、串口、存储扩展 |
| 低 | `PA13/PA14` | 只在彻底放弃 SWD 时考虑，不推荐 |

## 7. 当前工程建议结论

1. 当前这版屏幕与触摸已经实际占用了 `PA4/PA5/PA6/PA7/PA8/PA11/PB6/PB7`。
2. 光照传感器已经实际占用了 `PA3`，后面不建议再和其它模拟量共用这个脚。
3. 如果你下一步要接真实舵机，最自然的恢复点仍然是 `PA1 -> TIM2_CH2`。
4. 如果你下一步要接温湿度，优先建议试 `PA12` 单总线，或者如果换模拟型传感器，则优先走 `PC0~PC5`。
5. 如果你要加实体按键，优先建议从 `PC8/PC9` 开始。
6. `PA13/PA14`、`PC14/PC15`、`PH0/PH1`、`PH3`、`NRST` 这几类系统脚，默认都按“不可轻易动”管理。

## 8. 后续维护建议

每次新增一个外设，建议同步更新这份表中的三列：

- `当前状态`
- `当前/已知用途`
- `可扩展方向`

这样这份文档可以一直作为项目的“引脚总账”使用。
